You are a senior full-stack engineer, performance expert, and security specialist.

I have a React-based website hosted on Replit. I want to improve performance, security, UX, and compliance WITHOUT breaking any existing functionality or losing any data (pages, products, content).

CRITICAL REQUIREMENTS:
1. DO NOT remove or break existing pages, routes, or product data.
2. Maintain current design while improving performance and UX.
3. All improvements must be backward-compatible.
4. Ensure production-ready implementation.

----------------------------------
⚡ PERFORMANCE OPTIMIZATION
----------------------------------

1. Convert all images to modern formats:
   - Use AVIF (preferred) or WebP fallback
2. Implement responsive images:
   - srcset and sizes attributes
3. Add lazy loading:
   - loading="lazy" for below-the-fold images
4. Optimize Largest Contentful Paint (LCP):
   - Preload hero images
5. Enable code splitting and dynamic imports
6. Minimize JS bundle size
7. Add caching strategy for assets
8. Target performance:
   - Load time under 2 seconds
   - Lighthouse score 90+

----------------------------------
🔐 SECURITY HEADERS
----------------------------------

Add the following HTTP security headers:

1. Content-Security-Policy (CSP)
2. Strict-Transport-Security (HSTS)
3. X-Frame-Options
4. X-Content-Type-Options
5. Referrer-Policy
6. Permissions-Policy

Implementation:
- Use Express middleware or Replit server config
- Ensure no breaking of existing scripts/styles

----------------------------------
📱 UX IMPROVEMENTS
----------------------------------

1. Mobile-first optimization:
   - Ensure proper responsiveness for all screen sizes
2. Improve spacing and typography:
   - Consistent padding/margin system
   - Readable font sizes and line height
3. Improve UI consistency:
   - Buttons, forms, cards
4. Accessibility (WCAG 2.2):
   - Add alt text to all images
   - Add labels to all forms
   - Ensure keyboard navigation
   - Fix color contrast issues
5. Ensure no layout shifts (CLS optimization)

----------------------------------
🍪 COOKIE COMPLIANCE (DPDPA + GDPR)
----------------------------------

1. Implement cookie consent system:
   - Categories:
     - Necessary
     - Analytics
     - Marketing
     - Preferences
2. Default:
   - Only necessary cookies enabled
3. Add:
   - Accept / Reject / Customize options
4. Integrate with:
   - Google Analytics (load only after consent)
5. Store user consent state
6. Ensure compliance with:
   - DPDPA (India)
   - GDPR (EU)

----------------------------------
OUTPUT REQUIRED:
----------------------------------

1. Step-by-step implementation plan
2. Code snippets for:
   - Image optimization
   - Lazy loading
   - Security headers
   - Cookie consent system
3. Files to modify in Replit project
4. Testing checklist:
   - Performance
   - Security
   - Accessibility
5. Rollback plan if anything breaks